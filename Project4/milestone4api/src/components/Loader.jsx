import React from 'react'

const Loader = ({ label }) => {
    return (
        <div className='loader-C'>
            <div className="lw">

                <div className="loader">{label}</div>
            </div>
        </div>
    )
}

export default Loader